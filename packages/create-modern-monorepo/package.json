{
  "name": "create-modern-monorepo",
  "version": "0.1.0",
  "bin": {
    "create-modern-monorepo": "src/index.ts"
  },
  "files": [
    "src",
    "template"
  ],
  "type": "module",
  "scripts": {
    "build:template": "bun scripts/build-template.ts",
    "prepack": "bun scripts/build-template.ts",
    "test": "bun test ./src",
    "clean": "bun exec \"rm -rf .turbo node_modules template\""
  },
  "dependencies": {
    "@clack/prompts": "^0.11.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "engines": {
    "node": ">=24"
  }
}
